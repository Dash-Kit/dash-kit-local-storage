name: dash_kit_local_storage
description: The DashKit component for the local storage
version: 1.0.0
homepage: https://github.com/Dash-Kit/dash-kit-local-storage

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  localstorage: ^3.0.2+5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: